#include <stdio.h>
enum day {sunday, monday, tuesday, wednesday, thursday, friday, saturday};

void main()
{
	enum day d = tuesday;
	/* 
	    -  day is enum Name
	    -  d is a the enum variable
	    -  tuesday is a valuename
	
	*/
	
	
	printf("The day number stored in d is %d\n", d);
	printf("The day number stored in sunday is %d", sunday);
}





